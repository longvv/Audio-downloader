# Audio File Generator from CSV

This project converts a list of English words from a CSV file into audio files using the `gtts` (Google Text-to-Speech) library.

## Install Required Libraries

Ensure you have `gtts` and `pandas` installed. You can install them using pip:

```bash
pip install gtts pandas